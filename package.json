{
  "name": "crypto-trade-tax-analyzer-web",
  "private": true,
  "version": "1.0.0",
  "description": "Modern full-stack crypto trade tax analyzer web application",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix client",
    "dev": "node ./node_modules/concurrently/dist/bin/concurrently.js -n server,client -c green,cyan \"npm run dev --prefix backend\" \"npm run dev --prefix client\"",
    "dev:server": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "test": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
